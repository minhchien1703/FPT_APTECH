<?php
    class Customer {
        public $id;
        public $name;
        public $age;
        public $address;
        public $dob;

        function __construct() 
        {
            // Phương thức khởi tạo (__construct()) hiện tại không làm gì cả. 
            // Tuy nhiên, phương thức này có thể được sử dụng để khởi tạo các giá trị mặc định hoặc thực hiện các thao tác khi một đối tượng Customer mới được tạo ra.
        }
    }
    // lớp Customer chính là để định nghĩa một object bao gồm các thuộc tính id, name, age, address, dob.
?>